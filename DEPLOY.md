# 公開手順(Vercel)

仲間にURLを配って、スマホから使ってもらえる状態にするまでの手順です。
無料枠のまま公開できます。所要時間は 15 分ほどです。

このアプリは **AI 背景アートを使わなくても、カード作成は全部動きます**。
まずは AI なし(費用ゼロ)で公開し、必要になったら後から足すのがおすすめです。

---

## 1. Vercel に登録する

1. https://vercel.com を開く
2. 「Sign Up」から **GitHub アカウントで登録**する
3. Vercel から GitHub リポジトリへのアクセス許可を求められるので、`KWLBASE` を許可する

## 2. リポジトリを取り込む

1. Vercel のダッシュボードで「Add New…」→「Project」
2. 一覧から `KWLshohei/KWLBASE` を選び「Import」
3. 設定画面が出ますが、**何も変更せず**「Deploy」を押す

Next.js のアプリだと自動で判別されるので、ビルド設定を触る必要はありません。

## 3. URL を確認して配る

2〜3 分でビルドが終わり、`kwlbase.vercel.app` のような URL が発行されます。
スマホで開いて動くことを確認したら、その URL を仲間に共有してください。

URL の変更やドメインの追加は、プロジェクトの Settings → Domains からできます。

---

## 更新のしかた

`main` ブランチに変更を push すると、Vercel が自動でビルドし直して公開版が更新されます。
特別な操作は要りません。

---

## AI 背景アートを後から足す場合

好きな味に合わせた背景イラストを AI で生成する機能です。**有料**です。

### 事前に必ずやること

1. https://platform.openai.com でアカウントを作る
2. **使用額の上限(Usage limits)を設定する** ← 必ず先に設定してください
3. API キーを発行する

### 費用について

生成 1 枚ごとに OpenAI へ課金されます。単価は変動するため、
OpenAI の料金ページで最新の金額を確認してください。

**公開 URL は誰でも開けます。** 合言葉などの制限を付けない限り、
リンクを知った第三者も生成できてしまい、費用が膨らむ可能性があります。
アプリには簡易的なレート制限(15分あたり15回)がありますが、
Vercel のような環境ではサーバーが複数に分かれるため、確実には効きません。

上限設定を入れたうえで、必要なら合言葉による制限の追加をご相談ください。

### 設定手順

1. Vercel のプロジェクト → Settings → Environment Variables
2. 以下を追加する

   | Name | Value |
   | --- | --- |
   | `OPENAI_API_KEY` | 発行した API キー |

3. Deployments タブから最新のデプロイを Redeploy する

キーを設定すると、アプリに「背景をつくる」ボタンが出るようになります。
生成に失敗した場合はエラーにはならず、好きな味の色から作るグラデーション背景に切り替わります。

### 生成が途中で止まる場合

無料プランには関数の実行時間の上限があります。画像生成が上限を超えると、
背景はグラデーションにフォールバックします(カード自体は問題なく作れます)。
毎回そうなる場合は、有料プランへの変更が必要です。

---

## 手元で動かす場合

```bash
npm install
npm run dev
```

http://localhost:3000 を開きます。
AI 背景も試すなら、`.env.local` に `OPENAI_API_KEY=...` を書いてから起動してください。
